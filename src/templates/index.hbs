<!DOCTYPE html>
<html lang="pl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>W drodze</title>
    <!--build:css css/styles.min.css-->
    <link rel="stylesheet" href="css/main.css">
    <!--endbuild-->
    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-108818656-1"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag() { dataLayer.push(arguments); }
        gtag('js', new Date());

        gtag('config', 'UA-108818656-1');
    </script>
</head>

<body>
    <div class="wrapper">
        <header class="header">
            <a href="#">
                <h1>W drodze...</h1>
                <p class="header__subtitle">cykl autorskich wystaw zbiorowych Danuty Joppek</p>
            </a>
        </header>
        <main class="main">
            <article class="latest-event">
                <header>
                    {{#with pages.[0]}}
                        <a href="{{url}}">
                            <h2>{{title}} - {{location.city}}</h2>
                            <p class="latest-event__date">
                                {{#if date}}
                                    <time datetime="{{date.start}}">{{date.start}}</time>
                                    {{#if date.finish}} –
                                        <time datetime="{{date.finish}}">{{date.finish}}</time>
                                    {{/if}}
                                {{else}}Brak daty{{/if}}
                            </p>
                        </a>
                </header>
                <section class="latest-event__description">{{{contents}}}</section>
                {{#if artists}}
                    <h3>Artyści</h3>
                    <ul class="latest-event__artists">
                        {{#each artists}}
                            <li>
                                {{#if this.page}}
                                    <a href="artists/{{this.page}}">{{this.name}}</a>
                                {{else}}{{this.name}}{{/if}}
                            </li>
                        {{/each}}
                    </ul>
                {{/if}}
                {{#if sponsors}}
                    <h3>Patronat</h3>
                    <ul class="latest-event__sponsor-list">
                        {{#each sponsors}}
                            <li>
                                {{#if this.link}}
                                    <a class="latest-event__sponsor" href="{{this.link}}">
                                        {{#if this.logo}}
                                            <img class="latest-event__sponsor-logo" src="img/{{this.logo}}" alt="" title="{{this.name}}">
                                        {{/if}}
                                        <span>{{this.name}}</span>
                                    </a>
                                {{else}}{{this.name}}{{/if}}
                            </li>
                        {{/each}}
                    </ul>
                {{/if}}
                {{#if location}}
                    <h3>Miejsce</h3>
                    {{#if location.link}}
                        <a href="{{location.link}}">
                    {{/if}}
                    <p>{{location.name}}</p>
                    {{#if location.link}}
                        </a>
                        <a href="https://www.google.com/maps/search/?api=1&query={{{address_google location.address}}}">{{{address location.address}}}</a>
                    {{/if}}
                {{/if}}
                {{/with}}
            </article>
            <aside class="previous-events">
                <h2>Poprzednie spotkania</h2>
                {{#checklength pages 2}}
                    <ul class="previous-events__list">
                        {{#each pages}}
                            {{#unless @first}}
                                <li>
                                    <a class="previous-events__link" href="{{url}}">
                                        <span>{{title}}</span>
                                        <span class="previous-events__date">
                                            {{#if date}}
                                                <time datetime="{{date}}">{{date}}</time>
                                            {{else}}Brak daty{{/if}}
                                        </span>
                                    </a>
                                    <p class="previous-events__excerpt">{{excerpt}}</p>
                                </li>
                            {{/unless}}
                        {{/each}}
                    </ul>
                    {{else}}
                        <p>Brak wcześniejszych spotkań</p>
                {{/checklength}}
            </aside>
        </main>
        <footer class="footer">
            <small>Marek Sładczyk 2017</small>
        </footer>
    </div>
</body>

</html>